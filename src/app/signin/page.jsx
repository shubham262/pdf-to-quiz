import Login from "@/views/Login";

export const metadata = {
	title: "Sign In to get started",
	description: "Sign in to your account, to start generating Quizzes",
};

export default function Home() {
	return <Login />;
}
