import Landing from "@/views/Landing";

export const metadata = {
	title: "PDF to Quiz Generation",
	description: "Upload your pdf, and generate a simplified Quiz",
};

export default function Home() {
	return <Landing />;
}
